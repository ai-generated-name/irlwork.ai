# =============================================================================
# Stripe Connect V2 Sample — Environment Variables
# =============================================================================
#
# Copy this file to .env and fill in your Stripe credentials.
#
#   cp .env.example .env
#
# Find your keys at: https://dashboard.stripe.com/apikeys
# =============================================================================

# Your Stripe secret key (starts with sk_test_ for test mode)
# Required — the server will not start without this.
STRIPE_SECRET_KEY=sk_test_REPLACE_ME

# Webhook signing secret (starts with whsec_)
# Required for webhook signature verification.
#
# Option A: Get from Dashboard > Developers > Webhooks > your endpoint > Signing secret
# Option B: Run `stripe listen --print-secret` with the Stripe CLI
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# Server port (optional, defaults to 4242)
PORT=4242
